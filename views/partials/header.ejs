<nav class="navbar navbar-expand-lg bg-primary bg-gradient">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">
            <img src="images/Logo.png" width="48" height="48">
        </a>
        <!--* This button is here to be used when the screen is to small to fit in the device --->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <!--* Use collapse navbar so it can be changed to a nav menu if its in a cellphone --->
        <!--todo configure js to make the collapsing, Currently not responsive -->
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <% for(const page in pagesData) { %>
                    <li class="nav-item">
                        <a href="<%= pagesData[page].link %>" class="nav-link <%= pagesData[page].name == currentPage ? "active" : "" %>">
                            <%= pagesData[page].displayName %>
                        </a>
                    </li>
                    <% } %>  
            </ul>
        </div>
</nav>