<!DOCTYPE html>
<html lang="en">

<head>
    <%- include("../partials/head", {title: "Home" }) %>
</head>

<body>
    <%- include("../partials/header", { pagesData: pagesData }) %>
    <div class="container">
        <div class="row">
            <% projectsData.forEach(project => { %>
                <div class="col-sm-6">
                    <%- include("../partials/projectCard", {project: project}) %> 
                </div>
            <% }); %> 
        </div>
    </div>
</body>

</html>